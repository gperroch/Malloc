#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int			main(int argc, char **argv)
{
	char	*ptr;
	long int		size;
	size_t	sizet;
	int		choice;

	ptr = NULL;
	printf("sizeof(void*):%lu\n", sizeof(void*));
	return (0);
}
